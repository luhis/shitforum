@page "{boardKey}"
@model ShitForum.Pages.CatalogModel
@{
    ViewData["Title"] = "Board";
}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-page="Index">Home</a></li>
        <li class="breadcrumb-item active" aria-current="page">Catalog @Model.Board.BoardName</li>
    </ol>
</nav>
<hr />
[<a asp-page="Board" asp-route-id="@Model.Board.Id">Return</a>]
<hr />
<div class="row">
    @foreach (var thread in Model.Threads)
    {
        <div class="col-4">
            <div class="card">
                <a asp-page="Thread" asp-route-threadId="@thread.ThreadId" asp-route-subject="@thread.Subject">
                    <img class="card-img-top" src="@Url.Action("GetPostThumbnail", "Images", new { id = thread.OP.Id })" alt="@thread.Subject">
                </a>
                <div class="card-body">
                    <span class="card-title">
                        @thread.Subject
                        @thread.OP.Name
                        <vc:date-time-view p="thread.OP" />
                        No. <a asp-page="Thread" asp-route-id="@thread.ThreadId"><vc:friendly-guid g="thread.ThreadId" /></a>
                    </span>
                    <p class="card-text">@thread.OP.Comment</p>
                </div>
            </div>
        </div>
    }
</div>
