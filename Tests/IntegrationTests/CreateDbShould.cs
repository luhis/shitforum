using Persistence;
using Xunit;

namespace IntegrationTests
{
    public class CreateDbShould
    {
        [Fact]
        public void Create()
        {
            ////var cf = new ClientFactory(TestConfiguration.GetConfig());
            ////cf.CreateDb().Wait();
            ////cf.AddInitialData().Wait();
        }
    }
}